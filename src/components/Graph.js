const Graph = ({ graphData }) => {
    return (
        <>
            <p>I'm a graph</p>
            {/* <ul>
                {Object.keys(graphData).map((keyName) => (
                    <li key={keyName}>{keyName} : {graphData[keyName]}</li>
                ))}
            </ul> */}
        </>
    )
}

export default Graph
